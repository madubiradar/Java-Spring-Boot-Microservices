spring:
  application:
    name: Java-Spring-Boot-Microservices

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
    hibernate.ddl-auto: update

server:
  port: 8081